# 图像的直方图实验
*实验心得：		
打开一个文件可以按照默认也可以按照输入的参数来决定，一般是按照绝对路径，而且注意绝对路径的输入格式		
在处理图片时不需要将其转化为灰阶图像，使用一个vector<Mat>类型的变量存储来存储BGR三个通道的图像，经
过直方图计算后的结果存储在三个不同的Mat变量中，注意calcHist()函数中的通道数，是从0开始数的，如果为0代表单通道		
量化函数为normalize，是将计算后的图像量化在一定的范围		
最后则是进行统计和画图，统计使用的是cvRound(b_hist.at<float>(i))，是得到i值附近大小的点的个数		